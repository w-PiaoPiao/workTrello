### 桌宠看板 v0.2.0

Windows / macOS **桌面悬浮小工具**：缩小时是一只卡通桌宠，展开是清爽的 Trello 风格看板。

**本版更新 —— 补齐看板工具核心能力**

- 🗂 **多看板**：标题即切换器，新建 / 重命名 / 删除看板随取随用；
  每块看板独立文件存储，旧数据首次启动自动迁移（原 `board.json` 留档不删）
- ☑ **卡片清单**：可勾选子任务，卡面实时显示 ☑ 进度（全勾完转绿）
- 🖱 **多选批量操作**：Ctrl/Cmd+单击多选、Shift+单击范围选中，
  底部操作栏一键批量完成 / 打标签 / 移动 / 归档 / 删除（Esc 取消）；右键"复制卡片"
- 📅 **日历视图**：月历查看各截止日卡片，点开编辑、拖到另一天即改期
- 📎 **附件**：添加文件或直接粘贴剪贴板图片入卡，📎 徽章一键打开
- 📥 **导入**：支持 **Trello 看板导出 JSON** 与 **Markdown 任务清单**（含自家备份），
  列表 / 卡片 / 标签 / 清单 / 截止日完整迁移为一块新看板
- ⏰ **提醒增强**：截止日可提前 1~3 天提醒（设置「提醒」中配置）
- ↩️ **重做**：撤销之后可重做（⌘⇧Z / Ctrl+Y），新增 `?` 快捷键速查面板
- 📝 **Markdown 备注**：标题 / 加粗 / 待办等语法，「👁 预览」即时渲染
- 🔁 **重复任务扩展**：每天 / 每周 / 工作日 / 每月 / 每年 / 自定义 N 天，
  月末与闰日自动钳制

**核心功能**
- 🐾 桌宠：漂浮 / 呼吸 / 眨眼 / 随机小动作，4 款皮肤（奶糖 / 雪团 / 可可 / 子夜），完成彩蛋与状态表情
- 📋 看板：卡片与整列拖拽、重复任务、优先级 P1-P3、6 色标签、备注、截止日期与逐卡提醒
- ⭐ 今日聚焦：一眼看清今天要做什么；不展开看板也能在「今日清单」浮窗勾选
- 🍅 番茄钟专注：25 分钟倒计时，桌宠陪你打瞌睡，完成 🍅+1
- ⌘ 撤销 / 重做 20 步、实时搜索、归档恢复、Markdown / CSV 导出与完整备份、深浅色双主题

**安装**
- Windows：运行 `peTTrello-v0.2.0.exe`（单文件，免安装）
- macOS：解压 `peTTrello-v0.2.0.zip` 拖入「应用程序」（内含 `peTTrello-v0.2.0.app`）；未签名应用首次打开请右键 →「打开」

数据保存在本地（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`），自动备份 + 损坏恢复。

---

### 桌宠看板 v0.1.3

Windows / macOS **桌面悬浮小工具**：缩小时是一只卡通桌宠，展开是清爽的 Trello 风格看板。

> ⚠️ 本版修复了一个**数据丢失级缺陷**，请勿使用 0.1.2 及更早版本录入数据。

**本版更新**

- 🐛 **修复编辑不落盘**（严重）：数据变更后漏标脏标记，`flush()` 因"无变化"
  直接返回，所有增删改只停留在内存、进程退出即丢失，磁盘上永远停在首次
  启动那份默认空板。现已在数据变更的统一入口补上标脏，并新增回归护栏
  （改数据 → 落盘 → 重新读盘比对）
- 🔧 新增 3 项落盘回归用例（标脏、编辑落盘、改卡落盘），全量 208 项

**核心功能**
- 🐾 桌宠：漂浮 / 呼吸 / 眨眼 / 随机小动作，4 款皮肤（奶糖 / 雪团 / 可可 / 子夜），完成彩蛋与状态表情
- 📋 看板：卡片与整列拖拽、重复任务（每日 / 每周自动滚动）、优先级 P1-P3、6 色标签、备注、截止日期与逐卡提醒
- ⭐ 今日聚焦：一眼看清今天要做什么；不展开看板也能在「今日清单」浮窗勾选
- 🍅 番茄钟专注：25 分钟倒计时，桌宠陪你打瞌睡，完成 🍅+1
- ⌘ 撤销 20 步、实时搜索、归档恢复、Markdown / CSV 导出与完整备份、深浅色双主题

**安装**
- Windows：运行 `peTTrello-v0.1.3.exe`（单文件，免安装）
- macOS：解压 `peTTrello-v0.1.3.zip` 拖入「应用程序」（内含 `peTTrello-v0.1.3.app`）；未签名应用首次打开请右键 →「打开」

数据保存在本地（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`），自动备份 + 损坏恢复。

