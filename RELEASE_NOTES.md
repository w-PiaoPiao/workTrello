### 桌宠看板 v0.1.2

Windows / macOS **桌面悬浮小工具**：缩小时是一只卡通桌宠，展开是清爽的 Trello 风格看板。

> 本版合并了 0.1.1 与 0.1.2 两轮改动（0.1.1 未单独发布）。

**本版更新**

- 🎨 应用图标「桌宠探头」：矢量绘制，托盘 / 窗口 / 打包图标统一
- ✨ 过渡动画：列折叠展开、卡片增删、备注与今日清单浮窗、悬停删除按钮
  均为平滑淡入淡出；「暂停动画」开关一键关闭全部动效
- ⚡ 主题切换大幅提速（1500 张卡片实测 3.6 秒 → 5 毫秒）
- 🐛 修复卡片徽章不刷新：改优先级 / 番茄钟 / 重复周期后，卡片会一直显示旧值
- 🐛 修复列折叠展开后「添加卡片」按钮消失（QMenu 无 isOpen 致状态同步被截断）
- 🐛 修复列表颜色标记每次重启都换色（改用跨进程稳定的散列）
- 🐛 修复展开后列高不满、列头悬停闪动、折叠钮过小等看板交互细节
- 🔧 标签改为卡片左缘竖向色条，不再占用内容行
- 🔧 新增自动化测试流水线（Windows / macOS 双平台离屏跑 195 项用例）

**核心功能**
- 🐾 桌宠：漂浮 / 呼吸 / 眨眼 / 随机小动作，4 款皮肤（奶糖 / 雪团 / 可可 / 子夜），完成彩蛋与状态表情
- 📋 看板：卡片与整列拖拽、重复任务（每日 / 每周自动滚动）、优先级 P1-P3、6 色标签、备注、截止日期与逐卡提醒
- ⭐ 今日聚焦：一眼看清今天要做什么；不展开看板也能在「今日清单」浮窗勾选
- 🍅 番茄钟专注：25 分钟倒计时，桌宠陪你打瞌睡，完成 🍅+1
- ⌘ 撤销 20 步、实时搜索、归档恢复、Markdown / CSV 导出与完整备份、深浅色双主题

**安装**
- Windows：运行 `peTTrello-v0.1.2.exe`（单文件，免安装）
- macOS：解压 `peTTrello-v0.1.2.zip` 拖入「应用程序」（内含 `peTTrello-v0.1.2.app`）；未签名应用首次打开请右键 →「打开」

数据保存在本地（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`），自动备份 + 损坏恢复。
