### 桌宠看板 v0.2.1

Windows / macOS **桌面悬浮小工具**：缩小时是一只卡通桌宠，展开是清爽的 Trello 风格看板。

> 本版以修复为主：修掉一个"按钮点了没反应"的阻断缺陷与一批 UI/UX 问题，
> 并新增开机自启动。

**本版更新**

- 🚀 **开机自启动**：设置「窗口」中一键开启，登录系统后自动以桌宠形态常驻托盘。
  登记项写在当前用户范围（Windows 注册表 `Run`、macOS `~/Library/LaunchAgents`、
  Linux `~/.config/autostart`），无需管理员权限；写失败（权限/企业策略）会回滚
  开关并提示，不会留下"看起来开着、实际起不来"的假状态；程序换目录后启动时
  自动把登记的命令刷新到新路径
- 📁 **工作目录记住上次位置**：卡片编辑对话框的「浏览…」与卡片右键「设置工作目录」
  都从上次选过的目录打开（卡片自身已有目录则优先），路径失效——如移动硬盘拔了
  ——才回落到用户主目录
- 🎨 **标签色块更清爽**：卡片编辑对话框的 6 色标签去掉块内小字（挤在 34×22 的
  色块里被前景色压暗、几乎看不清），色名改由悬停提示给出，选中态看加粗边框
- ⚡ **展开不再卡顿**：折叠态启动跳过的看板控件改为展开时分帧构建——先建首列让
  动画立即起播，其余列逐帧补齐（此前 500 卡全量同步构建实测 400ms+，把
  "点击桌宠 → 展开动画起播"整段卡死）

**本次修复**

- 🐛 **设置界面换肤后旧皮肤仍显示选中**：四个皮肤按钮各自 checkable、没有互斥组，
  点新皮肤时旧的不会取消——皮肤实际换了（偏好已存），界面却像没换；从设置页
  换肤时，桌宠右键菜单里的旧皮肤也继续显示勾选。现改为单选组 + 三个入口
  （菜单 / 设置页 / 持久化偏好）共用同一份真相
- 🐛 **设置对话框打不开**：切「窗口置顶」会重建主窗口的原生窗口句柄，子对话框
  被一并隐藏——而设置对话框是**模态**的，窗口没了、模态阻塞还在，此后点击全被
  吞掉，看起来就是"点设置没反应"。现在切置顶会保住当时开着的对话框；另外
  "打开设置"时的偏好回填改为**静音同步**，不再反向触发置顶/动画等副作用
- 🐛 **清单项与附件没有删除按钮**：两处「✕」的样式只写了透明背景 + `border: none`
  而不给任何盒模型属性，Qt 于是把按钮的文字绘制区算成零尺寸——按钮存在、也点得到，
  但一个像素都不画，看起来就是"能加清单项、却没有删除的地方"。已统一到一处样式
  并补上显式 padding，两处 ✕ 恢复正常显示
- 🐛 **工具栏「+ 添加列表」点了没反应**（阻断）：漏导入对话框类，异常被 Qt 槽
  函数吞掉而无任何提示；macOS 菜单栏的同名入口同样失效
- 🐛 **归档恢复按钮不可达**：长标题把「恢复」按钮挤出可视区，现按宽度省略
- 🐛 **卡片删除按钮「✕」偶发不消失**：悬停态漏投递 Leave 事件
- 🐛 **日历视图**：英文界面星期表头仍是中文；「还有 N 项…」是不可点的死标签
  （被折叠的卡片再也打不开）；网格行高被内容撑高；条目顺序错乱
- 🐛 **窄窗工具栏叠字**：统计、日历、导出等次要入口按优先级收纳进「»」菜单，
  看板名按可用宽度省略
- 🐛 **英文界面下托盘无法切换显示/隐藏**：此前靠菜单文案比对判断状态，英文
  文案与中文比对必然失败
- 🐛 **批量添加剪贴板多行预填必崩**：计数标签晚于 `textChanged` 创建
- 🐛 **「暂停动画」总开关补齐 4 处漏检**：窗口展开/折叠、Toast 淡出、滑动开关、
  桌宠悬停弹跳
- 🔧 监听器改弱引用并自动剔除已失效回调：根治"切语言销毁日历后再切主题抛
  RuntimeError"与监听器泄漏
- 🔧 macOS 打包脚本在 CI 下必然失败（`$PYI，` 被 bash 并进变量名，`set -u` 直接报错）
- 其他：卡片左缘标签筛选补提示与操作反馈、日历按钮改自绘线性图标、快捷键面板
  补列 Ctrl+W 与 Ctrl+Shift+Z、Windows/Linux 补 Ctrl+W 收起为桌宠

**核心功能**

- 🐾 桌宠：漂浮 / 呼吸 / 眨眼 / 随机小动作，4 款皮肤（奶糖 / 雪团 / 可可 / 子夜），完成彩蛋与状态表情
- 📋 看板：多看板、卡片与整列拖拽、多选批量操作、卡片清单、附件、重复任务、优先级 P1-P3、6 色标签、备注、截止日期与逐卡提醒
- 📅 日历视图、Trello JSON / Markdown 导入、Markdown / CSV 导出与完整备份
- ⭐ 今日聚焦：一眼看清今天要做什么；不展开看板也能在「今日清单」浮窗勾选
- 🍅 番茄钟专注：25 分钟倒计时，桌宠陪你打瞌睡，完成 🍅+1
- ⌘ 撤销 / 重做 20 步、实时搜索、标签筛选、归档恢复、深浅色双主题、中英双语

**安装**

- Windows：运行 `peTTrello-v0.2.1.exe`（单文件，免安装）
- macOS：解压 `peTTrello-v0.2.1.zip` 拖入「应用程序」（内含 `peTTrello-v0.2.1.app`）；未签名应用首次打开请右键 →「打开」

数据保存在本地（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`），自动备份 + 损坏恢复。

---

### 桌宠看板 v0.2.0

Windows / macOS **桌面悬浮小工具**：缩小时是一只卡通桌宠，展开是清爽的 Trello 风格看板。

**本版更新 —— 补齐看板工具核心能力**

- 🗂 **多看板**：标题即切换器，新建 / 重命名 / 删除看板随取随用；
  每块看板独立文件存储，旧数据首次启动自动迁移（原 `board.json` 留档不删）
- ☑ **卡片清单**：可勾选子任务，卡面实时显示 ☑ 进度（全勾完转绿）
- 🖱 **多选批量操作**：Ctrl/Cmd+单击多选、Shift+单击范围选中，
  底部操作栏一键批量完成 / 打标签 / 移动 / 归档 / 删除（Esc 取消）；右键"复制卡片"
- 📅 **日历视图**：月历查看各截止日卡片，点开编辑、拖到另一天即改期
- 📎 **附件**：添加文件或直接粘贴剪贴板图片入卡，📎 徽章一键打开
- 📥 **导入**：支持 **Trello 看板导出 JSON** 与 **Markdown 任务清单**（含自家备份），
  列表 / 卡片 / 标签 / 清单 / 截止日完整迁移为一块新看板
- ⏰ **提醒增强**：截止日可提前 1~3 天提醒（设置「提醒」中配置）
- ↩️ **重做**：撤销之后可重做（⌘⇧Z / Ctrl+Y），新增 `?` 快捷键速查面板
- 📝 **Markdown 备注**：标题 / 加粗 / 待办等语法，「👁 预览」即时渲染
- 🔁 **重复任务扩展**：每天 / 每周 / 工作日 / 每月 / 每年 / 自定义 N 天，
  月末与闰日自动钳制

**本次修复**（0.2.0 开发期引入，未出现在任何发布版）

- 🐛 **修复编辑保存静默丢失**（严重）：卡片对话框此前设了 `WA_DeleteOnClose`，
  而它的销毁排在 `exec()` 嵌套事件循环退出时处理——`exec()` 返回时对话框
  连同子控件已析构，再去读表单结果必然抛异常，且槽函数里的异常只写日志、
  不冒泡：表现是"点了保存，内容全没存上"（备注最容易察觉，新建 / 编辑
  两个入口都中招）。现改为先取结果、再销毁对话框
- 🐛 **修复对话框内容显示不完整**：表单原先直接铺在窗口布局里，清单项 /
  附件一多就被压扁（清单输入行只剩 2px 高、还与备注框叠字），底部字段与
  按钮被挤出可视区。现表单区可滚动、保存按钮固定在底部，默认窗高按
  "空卡表单完整放下"取 660px
- 🔧 新增 8 项回归用例（保存链路 4 项含变异验证、表单滚动 4 项），全量 392 项

**核心功能**
- 🐾 桌宠：漂浮 / 呼吸 / 眨眼 / 随机小动作，4 款皮肤（奶糖 / 雪团 / 可可 / 子夜），完成彩蛋与状态表情
- 📋 看板：卡片与整列拖拽、重复任务、优先级 P1-P3、6 色标签、备注、截止日期与逐卡提醒
- ⭐ 今日聚焦：一眼看清今天要做什么；不展开看板也能在「今日清单」浮窗勾选
- 🍅 番茄钟专注：25 分钟倒计时，桌宠陪你打瞌睡，完成 🍅+1
- ⌘ 撤销 / 重做 20 步、实时搜索、归档恢复、Markdown / CSV 导出与完整备份、深浅色双主题

**安装**
- Windows：运行 `peTTrello-v0.2.0.exe`（单文件，免安装）
- macOS：解压 `peTTrello-v0.2.0.zip` 拖入「应用程序」（内含 `peTTrello-v0.2.0.app`）；未签名应用首次打开请右键 →「打开」

数据保存在本地（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`），自动备份 + 损坏恢复。

---

### 桌宠看板 v0.1.3

Windows / macOS **桌面悬浮小工具**：缩小时是一只卡通桌宠，展开是清爽的 Trello 风格看板。

> ⚠️ 本版修复了一个**数据丢失级缺陷**，请勿使用 0.1.2 及更早版本录入数据。

**本版更新**

- 🐛 **修复编辑不落盘**（严重）：数据变更后漏标脏标记，`flush()` 因"无变化"
  直接返回，所有增删改只停留在内存、进程退出即丢失，磁盘上永远停在首次
  启动那份默认空板。现已在数据变更的统一入口补上标脏，并新增回归护栏
  （改数据 → 落盘 → 重新读盘比对）
- 🔧 新增 3 项落盘回归用例（标脏、编辑落盘、改卡落盘），全量 208 项

**核心功能**
- 🐾 桌宠：漂浮 / 呼吸 / 眨眼 / 随机小动作，4 款皮肤（奶糖 / 雪团 / 可可 / 子夜），完成彩蛋与状态表情
- 📋 看板：卡片与整列拖拽、重复任务（每日 / 每周自动滚动）、优先级 P1-P3、6 色标签、备注、截止日期与逐卡提醒
- ⭐ 今日聚焦：一眼看清今天要做什么；不展开看板也能在「今日清单」浮窗勾选
- 🍅 番茄钟专注：25 分钟倒计时，桌宠陪你打瞌睡，完成 🍅+1
- ⌘ 撤销 20 步、实时搜索、归档恢复、Markdown / CSV 导出与完整备份、深浅色双主题

**安装**
- Windows：运行 `peTTrello-v0.1.3.exe`（单文件，免安装）
- macOS：解压 `peTTrello-v0.1.3.zip` 拖入「应用程序」（内含 `peTTrello-v0.1.3.app`）；未签名应用首次打开请右键 →「打开」

数据保存在本地（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`），自动备份 + 损坏恢复。

